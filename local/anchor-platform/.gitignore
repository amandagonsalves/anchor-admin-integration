# Generated by ap_start.sh from the .template files in config/. These embed a
# real (testnet-only, but still real) Stellar account, so only the .template
# versions with placeholders get committed.
config/assets.yaml
config/stellar.localhost.toml
